export default async function Model({ params }: { params: any }) {

    return (
      <main className="overflow-hidden w-full h-screen relative flex">
        <h1>Model to generate images</h1>
      </main>
    );
  }